#include <stdio.h>
int main()
{
    int a=10,b=5,c,*p1,*p2;
p1=&a;p2=&b;
c=*p1 + *p2;
printf("\n%d",c);
c=*p1 - *p2;
printf("\n%d",c);
c=*p1 * *p2;
printf("\n%d",c);
c=*p1 / *p2;
printf("\n%d",c);
c=*p1 % *p2;
printf("\n%d",c);
    return 0;
}
