#include<stdio.h>
int main()
{
    int a[5]={2,4,6,7,8},i,max;
    max=a[0];
    for(i=0;i<5;i++)
    {
        if(max<a[i])
        max=a[i];
    }
    printf("LARGEST VALUE =%d",max);
    return 0;
}
