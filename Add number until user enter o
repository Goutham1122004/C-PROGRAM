#include<stdio.h>
int main()
{
    int n,sum=0;
    do
    {
        printf("ENTER THE VALUE =");
        scanf("%d",&n);
        sum=sum+n;
    }
    while(n!=0);
    printf("ANSWER=%d",sum);
    return 0;
}
