#include <stdio.h>

int main()
{
    int a,c;
    scanf("%d",&a);
    c=sum(&a);
    printf("ANSWER=%d",c);
    return 0;
}
int sum(int*b)
{
    int d,sum=0,i;
    for(i=0;i<=*b;i++)
    {
       sum=sum+i;
    }
    return sum;
}
