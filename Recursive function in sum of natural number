#include <stdio.h>

int main()
{
   int a,b;
   scanf("%d",&a);
   b=sum(a);
   printf("%d",b);
   return 0;
}
int sum(int x)
{
    int y;
    if(x>0)
    {
        y=x+sum(x-1);
        return y;
    }
    else 
        return x;
}






