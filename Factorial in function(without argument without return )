#include <stdio.h>

int main()
{
    fact();
    return 0;
}
   void fact()
   {
       int i,n,f=1;
       printf("NUMBER=");
       scanf("%d",&n);
       for(i=1;i<=n;i++)
       {
           f=f*i;
       }
       printf("ANSWER=%d",f);
   }

