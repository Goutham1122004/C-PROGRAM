#include<stdio.h>
int main()
{
    int a,b;
    printf("NUMBER=");
    scanf("%d",&a);
    b=factorial(a);
    printf("ANSWER=%d",b);
    return 0;
}
int factorial(a)
{
   int fact=1,i;
   for(i=1;i<=a;i++)
   {
       fact=fact*i;
   }
   return fact;
}
