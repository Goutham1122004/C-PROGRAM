#include<stdio.h>
int main()
{
    int a,b,c;
    printf("A=");
    scanf("%d",&a);
    printf("B=");
    scanf("%d",&b);
    c=multiplication(a,b);
    printf("ANSWER=%d",c);
    return 0;
}
   int multiplication(a,b)
  {
    int d;
    d=a*b;
    return d;
  }
