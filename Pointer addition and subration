#include <stdio.h>
int main()
{
    int a,b;
    int *p,*q;
    printf("A=");
    scanf("%d",&a);
    printf("B=");
    scanf("%d",&b);
    p=&a;
    q=&b;
    printf("POINTER ADDITION=%d\n",*p+*q);
    printf("POINTER SUBRATION=%d",*p-*q);
    return 0;
}
