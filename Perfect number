#include <stdio.h>

int main()
{
 int n,i,sum=0;
 printf("NUMBER=");
 scanf("%d",&n);
 for(i=1;i<n;i++)
    if(n%i==0)
    sum=sum+i;
    printf("ANSWER=%d",sum);
     if(sum==n)
     printf("\nIT IS A PERFECT NUMBER");
     else
     printf("\nIT IS NOT A PERFECT NUMBER");
    return 0;
}
