#include<stdio.h>
int main()
{
    int i,n,a[10],sum=0;
    printf("N=");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        sum=sum+a[i];
    }
    printf("TOTAL ANSWER=%d",sum);
    return 0;
}
