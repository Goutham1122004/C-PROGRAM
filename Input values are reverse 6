#include<stdio.h>
int main()
{
    int n,sum=0;
    printf("ENTER THE VALUE=");
    scanf("%d",&n);
    while(n!=0)
    {
        sum=sum*10;
        sum=sum+n%10;
        n=n/10;
    }
    printf("ANSWER=%d",sum);
    return 0;
}
